<Page x:Class="CATRA.UI.Views.ProcessingQueueView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:CATRA.UI.Controls"
      xmlns:converters="clr-namespace:CATRA.UI.Converters"
      Background="{DynamicResource WindowBackgroundBrush}"
      KeepAlive="True"
      Title="CATRA — Fila">
    <Page.Resources>
        <converters:JobStatusToIconConverter x:Key="StatusToIcon" />
        <converters:EpisodeProcessStatusToBrushConverter x:Key="StatusToBrush" />
    </Page.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header: back + title + profile -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button VerticalAlignment="Center"
                    Command="{Binding GoBackCommand}"
                    Content="← Voltar"
                    Style="{StaticResource BaseButtonStyle}" />
            <TextBlock Margin="16,0,0,0"
                       VerticalAlignment="Center"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="{DynamicResource WindowForegroundBrush}">
                <Run Text="📥 Pre-Processamento —" />
                <Run Text="{Binding SeriesTitle, Mode=OneWay}" />
            </TextBlock>
            <TextBlock Margin="16,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource SectionHeaderForegroundBrush}"
                       Text="{Binding ActiveProfileLabel, Mode=OneWay}" />
        </StackPanel>

        <ScrollViewer Grid.Row="1"
                      Margin="0,12,0,0"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Disk usage -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <DockPanel>
                            <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="Disco" />
                            <TextBlock HorizontalAlignment="Right"
                                       DockPanel.Dock="Right"
                                       Foreground="{DynamicResource SectionHeaderForegroundBrush}">
                                <Run Text="{Binding DiskUsedLabel, Mode=OneWay}" />
                                <Run Text=" / " />
                                <Run Text="{Binding DiskEstimatedLabel, Mode=OneWay}" />
                            </TextBlock>
                        </DockPanel>
                        <ProgressBar Height="10"
                                     Margin="0,8,0,0"
                                     Maximum="100"
                                     Minimum="0"
                                     Value="{Binding DiskProgressPct, Mode=OneWay}" />
                    </StackPanel>
                </Border>

                <!-- Processing now -->
                <StackPanel Margin="0,12,0,0">
                    <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="Processando agora" />
                    <controls:ProcessJobCardControl Margin="0,6,0,0" />
                    <TextBlock Margin="0,6,0,0"
                               Foreground="{DynamicResource SectionHeaderForegroundBrush}"
                               Text="Nenhum job em execução.">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasCurrentJob}" Value="False">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <!-- Window episodes -->
                <StackPanel Margin="0,20,0,0">
                    <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="Na janela" />
                    <ItemsControl Margin="0,6,0,0" ItemsSource="{Binding WindowEpisodes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,2"
                                        Padding="8,6"
                                        Style="{StaticResource CardStyle}">
                                    <DockPanel>
                                        <TextBlock Width="24"
                                                   FontWeight="Bold"
                                                   Foreground="{Binding Status, Converter={StaticResource StatusToBrush}}"
                                                   Text="{Binding Status, Converter={StaticResource StatusToIcon}}" />
                                        <TextBlock Width="52"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource CardForegroundBrush}"
                                                   Text="{Binding Label}" />
                                        <TextBlock HorizontalAlignment="Right"
                                                   DockPanel.Dock="Right"
                                                   Foreground="{DynamicResource SectionHeaderForegroundBrush}"
                                                   Text="{Binding SizeLabel}" />
                                        <TextBlock Foreground="{DynamicResource SectionHeaderForegroundBrush}"
                                                   Text="{Binding Title}"
                                                   TextTrimming="CharacterEllipsis" />
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <!-- Footer -->
                <TextBlock Margin="0,20,0,0"
                           FontSize="12"
                           Foreground="{DynamicResource SectionHeaderForegroundBrush}"
                           Text="{Binding FooterText}"
                           TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
